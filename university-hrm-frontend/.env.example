# ─── University HRM Frontend — Environment Variables ──────────────────────────
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NOTE: CRA reads .env at BUILD time. After changing values, restart dev server.

REACT_APP_API_URL=http://localhost:8000/api/v1
